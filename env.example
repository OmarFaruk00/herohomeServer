PORT=5000
MONGODB_URI=your_mongodb_connection_string_here
FRONTEND_URL=http://localhost:3000

# Firebase Admin SDK Configuration
# Option 1: Use service account key (recommended for production)
# Download service account key from Firebase Console > Project Settings > Service Accounts
# Then paste the entire JSON content here (as a single line or use JSON.parse in code)
FIREBASE_SERVICE_ACCOUNT_KEY=

# Option 2: Use project ID (for development with application default credentials)
FIREBASE_PROJECT_ID=your_firebase_project_id_here
